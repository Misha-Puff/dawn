<link rel="stylesheet" href="{{ 'component-list-menu.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-search.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-menu-drawer.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'section-header.css' | asset_url }}" media="print" onload="this.media='all'">

<link
  rel="stylesheet"
  href="{{ 'component-cart-notification.css' | asset_url }}"
  media="print"
  onload="this.media='all'"
>

{%- if settings.predictive_search_enabled -%}
  <link rel="stylesheet" href="{{ 'component-price.css' | asset_url }}" media="print" onload="this.media='all'">
{%- endif -%}

{%- if section.settings.menu_type_desktop == 'mega' -%}
  <link rel="stylesheet" href="{{ 'component-mega-menu.css' | asset_url }}" media="print" onload="this.media='all'">
{%- endif -%}

<style>
  header-drawer {
    display: flex;
    width: fit-content;
    justify-self: start;
    margin-left: -1.2rem;
  }

  {%- if section.settings.sticky_header_type == 'reduce-logo-size' -%}
    .scrolled-past-header .header__heading-logo-wrapper {
      width: 75%;
    }
  {%- endif -%}

  {%- if section.settings.menu_type_desktop != "drawer" -%}
    @media screen and (min-width: 1100px) {
      header-drawer {
        display: none;
      }
    }
  {%- endif -%}

  .menu-drawer-container {
    display: flex;
    width: fit-content;
  }

  .list-menu {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .list-menu--inline {
    display: inline-flex;
    flex-wrap: wrap;
  }

  /* summary.list-menu__item {
    padding-right: 2.7rem;
  } */

  .list-menu__item {
    display: flex;
    align-items: center;
    line-height: calc(1 + 0.3 / var(--font-body-scale));
  }

  .list-menu__item--link {
    text-decoration: none;
    padding-bottom: 1rem;
    padding-top: 1rem;
    line-height: calc(1 + 0.8 / var(--font-body-scale));
  }

  .list-menu__item--link {
    padding-bottom: 0.5rem;
    padding-top: 0.5rem;
  }
</style>

{%- comment -%}
  Batch 2: optional nav typography overrides, split across three targets:
  main nav (top-level items), sub-nav headers (level 2), sub-nav items (level 3).
  Each group shares the same 5 controls, keyed by a prefix; the loop keeps it DRY.
  #shopify-section-<id> gives every rule an ID, so it beats the 13px base rules.
{%- endcomment -%}
{%- if section.settings.nav_enable_type -%}
  {%- capture nav_type_css -%}
    {%- assign nav_groups = 'mainnav|.header__menu-item,subhead|.mega-menu__link--level-2,subitem|.mega-menu__link:not(.mega-menu__link--level-2)' | split: ',' -%}
    {%- for grp in nav_groups -%}
      {%- assign grp_parts = grp | split: '|' -%}
      {%- assign gp = grp_parts[0] -%}
      {%- assign sel = grp_parts[1] -%}
      {%- assign k_font = gp | append: '_custom_font' -%}
      {%- assign k_weight = gp | append: '_font_weight' -%}
      {%- assign k_size = gp | append: '_font_size' -%}
      {%- assign k_case = gp | append: '_text_case' -%}
      {%- assign k_ls = gp | append: '_letter_spacing' -%}
      {%- capture grp_brand -%}{% render 'brand-font', font: section.settings[k_font] %}{%- endcapture -%}
      {%- assign grp_brand = grp_brand | strip -%}
      {%- capture grp_decl -%}
        {%- if grp_brand != blank %}font-family: {{ grp_brand }};{% endif -%}
        {%- if section.settings[k_weight] != 'default' and section.settings[k_weight] != blank %} font-weight: {{ section.settings[k_weight] }};{% endif -%}
        {%- if section.settings[k_size] > 0 %} font-size: {{ section.settings[k_size] }}px;{% endif -%}
        {%- if section.settings[k_case] != 'default' and section.settings[k_case] != blank %} text-transform: {{ section.settings[k_case] }};{% endif -%}
        {%- if section.settings[k_ls] > 0 %} letter-spacing: {{ section.settings[k_ls] }}px;{% endif -%}
      {%- endcapture -%}
      {%- assign grp_decl = grp_decl | strip -%}
      {%- if grp_decl != blank -%}
        #shopify-section-{{ section.id }} {{ sel }} { {{ grp_decl }} }
      {%- endif -%}
    {%- endfor -%}
  {%- endcapture -%}
  {%- assign nav_type_css = nav_type_css | strip -%}
  {%- if nav_type_css != blank -%}
    <style>
      {{ nav_type_css }}
    </style>
  {%- endif -%}
{%- endif -%}

{%- comment -%}
  Notes batch: header padding overrides + mega-menu list gaps. All 0-sentinel —
  nothing is emitted at defaults. #shopify-section-<id> scoping outranks the
  base.css .header.page-width breakpoint rules (ID beats any class chain).
{%- endcomment -%}
{%- capture header_space_css -%}
  {%- if section.settings.padding_top > 0 or section.settings.padding_bottom > 0 -%}
    #shopify-section-{{ section.id }} .header { {% if section.settings.padding_top > 0 %}padding-top: {{ section.settings.padding_top }}px;{% endif %}{% if section.settings.padding_bottom > 0 %} padding-bottom: {{ section.settings.padding_bottom }}px;{% endif %} }
  {%- endif -%}
  {%- if section.settings.padding_left_mobile > 0 or section.settings.padding_right_mobile > 0 -%}
    @media screen and (max-width: 749px) { #shopify-section-{{ section.id }} .header { {% if section.settings.padding_left_mobile > 0 %}padding-left: {{ section.settings.padding_left_mobile }}px;{% endif %}{% if section.settings.padding_right_mobile > 0 %} padding-right: {{ section.settings.padding_right_mobile }}px;{% endif %} } }
  {%- endif -%}
  {%- if section.settings.padding_left > 0 or section.settings.padding_right > 0 -%}
    @media screen and (min-width: 750px) { #shopify-section-{{ section.id }} .header { {% if section.settings.padding_left > 0 %}padding-left: {{ section.settings.padding_left }}px;{% endif %}{% if section.settings.padding_right > 0 %} padding-right: {{ section.settings.padding_right }}px;{% endif %} } }
  {%- endif -%}
  {%- if section.settings.megamenu_row_gap > 0 or section.settings.megamenu_col_gap > 0 -%}
    #shopify-section-{{ section.id }} { {% if section.settings.megamenu_row_gap > 0 %}--mm-list-row-gap: {{ section.settings.megamenu_row_gap }}px;{% endif %}{% if section.settings.megamenu_col_gap > 0 %} --mm-list-col-gap: {{ section.settings.megamenu_col_gap }}px;{% endif %} }
  {%- endif -%}
{%- endcapture -%}
{%- assign header_space_css = header_space_css | strip -%}
{%- if header_space_css != blank -%}
  <style>
    {{ header_space_css }}
  </style>
{%- endif -%}

{%- comment -%}
  Notes batch: mobile drawer per-tier typography/spacing under one gate, same
  shape as the nav loop above. Tier classes (--l1/--l2/--l3) are set in
  header-drawer.liquid; the CHILD/BABY/ADULT always-expanded branches share the
  same submenu markup, so the tier rules cover them too. Indent vars are read by
  component-menu-drawer.css (fallback 20px each = the historical margins).
{%- endcomment -%}
{%- if section.settings.drawer_enable_type -%}
  {%- capture drawer_type_css -%}
    {%- assign drawer_groups = 'drawer_l1|.menu-drawer__menu-item--l1,drawer_l2|.menu-drawer__menu-item--l2,drawer_l3|.menu-drawer__menu-item--l3' | split: ',' -%}
    {%- for grp in drawer_groups -%}
      {%- assign grp_parts = grp | split: '|' -%}
      {%- assign gp = grp_parts[0] -%}
      {%- assign sel = grp_parts[1] -%}
      {%- assign k_font = gp | append: '_custom_font' -%}
      {%- assign k_weight = gp | append: '_font_weight' -%}
      {%- assign k_size = gp | append: '_font_size' -%}
      {%- assign k_case = gp | append: '_text_case' -%}
      {%- assign k_ls = gp | append: '_letter_spacing' -%}
      {%- assign k_vs = gp | append: '_v_space' -%}
      {%- capture grp_brand -%}{% render 'brand-font', font: section.settings[k_font] %}{%- endcapture -%}
      {%- assign grp_brand = grp_brand | strip -%}
      {%- capture grp_decl -%}
        {%- if grp_brand != blank %}font-family: {{ grp_brand }};{% endif -%}
        {%- if section.settings[k_weight] != 'default' and section.settings[k_weight] != blank %} font-weight: {{ section.settings[k_weight] }};{% endif -%}
        {%- if section.settings[k_size] > 0 %} font-size: {{ section.settings[k_size] }}px;{% endif -%}
        {%- if section.settings[k_case] != 'default' and section.settings[k_case] != blank %} text-transform: {{ section.settings[k_case] }};{% endif -%}
        {%- if section.settings[k_ls] > 0 %} letter-spacing: {{ section.settings[k_ls] }}px;{% endif -%}
        {%- if section.settings[k_vs] > 0 %} padding-top: {{ section.settings[k_vs] }}px; padding-bottom: {{ section.settings[k_vs] }}px;{% endif -%}
      {%- endcapture -%}
      {%- assign grp_decl = grp_decl | strip -%}
      {%- if grp_decl != blank -%}
        #shopify-section-{{ section.id }} {{ sel }} { {{ grp_decl }} }
      {%- endif -%}
    {%- endfor -%}
    {%- if section.settings.drawer_l1_indent > 0 -%}
      #shopify-section-{{ section.id }} .menu-drawer__menu-item { padding-left: {{ section.settings.drawer_l1_indent }}px; }
    {%- endif -%}
    #shopify-section-{{ section.id }} { --mp-drawer-indent-l2: {{ section.settings.drawer_l2_indent }}px; --mp-drawer-indent-l3: {{ section.settings.drawer_l3_indent }}px; }
  {%- endcapture -%}
  {%- assign drawer_type_css = drawer_type_css | strip -%}
  {%- if drawer_type_css != blank -%}
    <style>
      {{ drawer_type_css }}
    </style>
  {%- endif -%}
{%- endif -%}

{% liquid
  assign paths = section.settings.transparency_enabled_paths | split: ','

  for path in paths
    if request.path == path
      assign transparency_enabled = true
    endif
  endfor
%}
{%- style -%}
  .header {
    padding: 10px 1.4rem;
  }

  .section-header {
    position: sticky; /* This is for fixing a Safari z-index issue. PR #2147 */
    {% if transparency_enabled %}
      margin-bottom: calc({{ section.settings.margin_bottom | times: 0.75 | round: 0 }}px - var(--header-height, 0px));
    {% else %}
      margin-bottom: {{ section.settings.margin_bottom | times: 0.75 | round: 0 }}px;
    {% endif %}
  }

  @media screen and (min-width: 750px) {
    .section-header {
      {% if transparency_enabled %}
        margin-bottom: calc({{ section.settings.margin_bottom }}px - var(--header-height, 0px));
      {% else %}
        margin-bottom: {{ section.settings.margin_bottom }}px;
      {% endif %}
    }
  }

  @media screen and (max-width: 1099px) {
    .header__heading-logo-wrapper {
      max-width: {{ settings.mobile_logo_width }}px;
    }
  }

  {% if settings.enable_sticky_bar %}
    .shopify-section-header-sticky {
      top: {{ settings.sticky_bar_offset }}px;
    }
  {% endif %}

  {% if transparency_enabled %}
    .section-header .header-wrapper {
      background-color: transparent;
    }

    .section-header .header-wrapper.gradient {
      background: transparent;
    }

    .scrolled-past-header .header-wrapper {
      background-color: rgb(var(--color-background));
    }

    .scrolled-past-header .header-wrapper.gradient {
      background: rgb(var(--color-background));
      background: var(--gradient-background);
      background-attachment: fixed;
    }

    .section-header .header-wrapper--border-bottom {
      border-bottom-color: transparent;
    }

    .scrolled-past-header .header-wrapper--border-bottom {
      border-bottom-color: rgba(var(--color-foreground), 0.08);
    }

    .section-header:not(.scrolled-past-header):not(:has(details[open])) .header-wrapper {
      --color-foreground: var(--color-background);
    }

    .section-header:not(.scrolled-past-header):not(:has(details[open])) .header__heading-logo {
      filter: invert(1);
    }

    .section-header:has(details[open]) .header-wrapper {
      background-color: rgb(var(--color-background));
    }

    .section-header:has(details[open]) .header-wrapper.gradient {
      background: rgb(var(--color-background));
      background: var(--gradient-background);
      background-attachment: fixed;
    }

    .section-header:has(details[open]) .header-wrapper--border-bottom {
      border-bottom-color: rgba(var(--color-foreground), 0.08);
    }

    .section-header .mega-menu__content {
      top: 100%;
      background-color: transparent;
    }

    .scrolled-past-header .mega-menu__content,
    .section-header:has(details[open]) .mega-menu__content {
      background-color: rgb(var(--color-background));
    }

    .mega-menu__content {
      top: 100%;
    }

    .header__icon .icon {
      fill: currentColor;
    }
  {% endif %}

  @media screen and (max-width: 1099px){
    .header__icons .header__icon .svg-wrapper {
      justify-content: flex-end;
    }
  }
{%- endstyle -%}

<script src="{{ 'cart-notification.js' | asset_url }}" defer="defer"></script>

{%- liquid
  for block in section.blocks
    if block.type == '@app'
      assign has_app_block = true
    endif
  endfor
-%}

{% liquid
  assign header_tag = 'div'

  if section.settings.sticky_header_type != 'none'
    assign header_tag = 'sticky-header'
  endif
%}

<{{ header_tag }}
  {% if header_tag == 'sticky-header' %}
    data-sticky-type="{{ section.settings.sticky_header_type }}"
  {% endif %}
  class="header-wrapper color-{{ section.settings.color_scheme }} gradient{% if section.settings.show_line_separator %} header-wrapper--border-bottom{% endif %}"
>
  {%- liquid
    assign social_links = false
    assign localization_forms = false

    if settings.social_twitter_link != blank or settings.social_facebook_link != blank or settings.social_pinterest_link != blank or settings.social_instagram_link != blank or settings.social_youtube_link != blank or settings.social_vimeo_link != blank or settings.social_tiktok_link != blank or settings.social_tumblr_link != blank or settings.social_snapchat_link != blank
      assign social_links = true
    endif

    if localization.available_countries.size > 1 and section.settings.enable_country_selector or section.settings.enable_language_selector and localization.available_languages.size > 1
      assign localization_forms = true
    endif
  -%}
  <header class="header header--{{ section.settings.logo_position }} header--mobile-{{ section.settings.mobile_logo_position }} page-width{% if section.settings.menu_type_desktop == 'drawer' %} drawer-menu{% endif %}{% if section.settings.menu != blank %} header--has-menu{% endif %}{% if has_app_block %} header--has-app{% endif %}{% if social_links %} header--has-social{% endif %}{% if shop.customer_accounts_enabled %} header--has-account{% endif %}{% if localization_forms %} header--has-localizations{% endif %}">
    <div class="left-icons-wrapper">
      {%- liquid
        if section.settings.menu != blank
          render 'header-drawer'
        endif

        render 'header-search', input_id: 'Search-In-Modal-1'
      -%}
    </div>

    {%- if section.settings.logo_position != 'middle-center' -%}
      {%- if request.page_type == 'index' -%}
        <h1 class="header__heading">
      {%- endif -%}
      <a href="{{ routes.root_url }}" class="header__heading-link link link--text focus-inset">
        {%- if settings.logo != blank -%}
          <div class="header__heading-logo-wrapper">
            {%- assign logo_alt = settings.logo.alt | default: shop.name | escape -%}
            {%- assign logo_height = settings.logo_width | divided_by: settings.logo.aspect_ratio -%}
            {% capture sizes %}(max-width: {{ settings.logo_width | times: 2 }}px) 50vw, {{ settings.logo_width }}px{% endcapture %}
            {% capture widths %}{{ settings.logo_width }}, {{ settings.logo_width | times: 1.5 | round }}, {{ settings.logo_width | times: 2 }}{% endcapture %}
            {{
              settings.logo
              | image_url: width: 600
              | image_tag:
                class: 'header__heading-logo motion-reduce',
                widths: widths,
                height: logo_height,
                width: settings.logo_width,
                alt: logo_alt,
                sizes: sizes,
                preload: true
            }}
          </div>
        {%- else -%}
          <span class="h2">{{ shop.name }}</span>
        {%- endif -%}
      </a>
      {%- if request.page_type == 'index' -%}
        </h1>
      {%- endif -%}
    {%- endif -%}

    {%- liquid
      if section.settings.menu != blank
        if section.settings.menu_type_desktop == 'dropdown'
          render 'header-dropdown-menu'
        elsif section.settings.menu_type_desktop != 'drawer'
          render 'header-mega-menu'
        endif
      endif
    %}

    {%- if section.settings.logo_position == 'middle-center' -%}
      {%- if request.page_type == 'index' -%}
        <h1 class="header__heading">
      {%- endif -%}
      <a href="{{ routes.root_url }}" class="header__heading-link link link--text focus-inset">
        {%- if settings.logo != blank -%}
          <div class="header__heading-logo-wrapper">
            {%- assign logo_alt = settings.logo.alt | default: shop.name | escape -%}
            {%- assign logo_height = settings.logo_width | divided_by: settings.logo.aspect_ratio -%}
            {% capture sizes %}(min-width: 750px) {{ settings.logo_width }}px, 50vw{% endcapture %}
            {% capture widths %}{{ settings.logo_width }}, {{ settings.logo_width | times: 1.5 | round }}, {{ settings.logo_width | times: 2 }}{% endcapture %}
            {{
              settings.logo
              | image_url: width: 600
              | image_tag:
                class: 'header__heading-logo',
                widths: widths,
                height: logo_height,
                width: settings.logo_width,
                alt: logo_alt,
                sizes: sizes,
                preload: true
            }}
          </div>
        {%- else -%}
          <span class="h2">{{ shop.name }}</span>
        {%- endif -%}
      </a>
      {%- if request.page_type == 'index' -%}
        </h1>
      {%- endif -%}
    {%- endif -%}

    <div class="header__icons{% if section.settings.enable_country_selector or section.settings.enable_language_selector %} header__icons--localization header-localization{% endif %}">
      <div class="desktop-localization-wrapper">
        {%- if section.settings.enable_country_selector and localization.available_countries.size > 1 -%}
          <localization-form class="small-hide medium-hide" data-prevent-hide>
            {%- form 'localization', id: 'HeaderCountryForm', class: 'localization-form' -%}
              <div>
                <h2 class="visually-hidden" id="HeaderCountryLabel">{{ 'localization.country_label' | t }}</h2>
                {%- render 'country-localization', localPosition: 'HeaderCountry' -%}
              </div>
            {%- endform -%}
          </localization-form>
        {% endif %}

        {%- if section.settings.enable_language_selector and localization.available_languages.size > 1 -%}
          <localization-form class="small-hide medium-hide" data-prevent-hide>
            {%- form 'localization', id: 'HeaderLanguageForm', class: 'localization-form' -%}
              <div>
                <h2 class="visually-hidden" id="HeaderLanguageLabel">{{ 'localization.language_label' | t }}</h2>
                {%- render 'language-localization', localPosition: 'HeaderLanguage' -%}
              </div>
            {%- endform -%}
          </localization-form>
        {%- endif -%}
      </div>
      {% render 'header-search', input_id: 'Search-In-Modal' %}

      {%- if shop.customer_accounts_enabled -%}
        <a
          href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
          class="header__icon header__icon--account link focus-inset"
          rel="nofollow"
        >
          {%- if section.settings.enable_customer_avatar -%}
            <account-icon>
              {%- if customer and customer.has_avatar? -%}
                {{ customer | avatar }}
              {%- else -%}
                <span class="svg-wrapper">{{ 'icon-account.svg' | inline_asset_content }}</span>
              {%- endif -%}
            </account-icon>
          {%- else -%}
            <span class="svg-wrapper">{{ 'icon-account.svg' | inline_asset_content }}</span>
          {%- endif -%}
          <span class="visually-hidden">
            {%- liquid
              if customer
                echo 'customer.account_fallback' | t
              else
                echo 'customer.log_in' | t
              endif
            -%}
          </span>
        </a>
      {%- endif -%}

      <a href="/pages/swym-wishlist" class="header__icon link focus-inset">
        <span class="svg-wrapper">{{- 'icon-heart.svg' | inline_asset_content -}}</span>
      </a>

      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when '@app' -%}
            {% render block %}
        {%- endcase -%}
      {%- endfor -%}

      <a href="{{ routes.cart_url }}" class="header__icon header__icon--cart link focus-inset" id="cart-icon-bubble">
        {% if cart == empty %}
          <span class="svg-wrapper">{{ 'icon-cart-empty.svg' | inline_asset_content }}</span>
        {% else %}
          <span class="svg-wrapper">{{ 'icon-cart.svg' | inline_asset_content }}</span>
        {% endif %}
        <span class="visually-hidden">{{ 'templates.cart.cart' | t }}</span>
        {%- if cart != empty -%}
          <div class="cart-count-bubble">
            {%- if cart.item_count < 100 -%}
              <span aria-hidden="true">{{ cart.item_count }}</span>
            {%- endif -%}
            <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
          </div>
        {%- endif -%}
      </a>
    </div>
  </header>
</{{ header_tag }}>

{% if transparency_enabled %}
  <script>
    (function() {
      var header = document.querySelector('.section-header');
      if (header) {
        document.documentElement.style.setProperty('--header-height', header.offsetHeight + 'px');
      }
    })();
  </script>
{% endif %}

{%- if settings.cart_type == 'notification' -%}
  {%- render 'cart-notification',
    color_scheme: section.settings.color_scheme,
    desktop_menu_type: section.settings.menu_type_desktop
  -%}
{%- endif -%}

{% javascript %}
  class StickyHeader extends HTMLElement {
    constructor() {
      super();
    }

    connectedCallback() {
      this.header = document.querySelector('.section-header');
      this.headerIsAlwaysSticky =
        this.getAttribute('data-sticky-type') === 'always' ||
        this.getAttribute('data-sticky-type') === 'reduce-logo-size';
      this.headerBounds = {};

      this.setHeaderHeight();

      window.matchMedia('(max-width: 1100px)').addEventListener('change', this.setHeaderHeight.bind(this));

      if (this.headerIsAlwaysSticky) {
        this.header.classList.add('shopify-section-header-sticky');
      }

      this.currentScrollTop = 0;
      this.preventReveal = false;
      this.predictiveSearch = this.querySelector('predictive-search');

      this.onScrollHandler = this.onScroll.bind(this);
      this.hideHeaderOnScrollUp = () => (this.preventReveal = true);

      this.addEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.addEventListener('scroll', this.onScrollHandler, false);

      this.createObserver();
    }

    setHeaderHeight() {
      document.documentElement.style.setProperty('--header-height', `${this.header.offsetHeight}px`);
    }

    disconnectedCallback() {
      this.removeEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.removeEventListener('scroll', this.onScrollHandler);
    }

    createObserver() {
      let observer = new IntersectionObserver((entries, observer) => {
        this.headerBounds = entries[0].intersectionRect;
        observer.disconnect();
      });

      observer.observe(this.header);
    }

    onScroll() {
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

      if (this.predictiveSearch && this.predictiveSearch.isOpen) return;

      if (scrollTop > this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (this.preventHide) return;
        requestAnimationFrame(this.hide.bind(this));
      } else if (scrollTop < this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (!this.preventReveal) {
          requestAnimationFrame(this.reveal.bind(this));
        } else {
          window.clearTimeout(this.isScrolling);

          this.isScrolling = setTimeout(() => {
            this.preventReveal = false;
          }, 66);

          requestAnimationFrame(this.hide.bind(this));
        }
      } else if (scrollTop <= this.headerBounds.top) {
        this.header.classList.remove('scrolled-past-header');
        requestAnimationFrame(this.reset.bind(this));
      }

      this.currentScrollTop = scrollTop;
    }

    hide() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-hidden', 'shopify-section-header-sticky');
      this.closeMenuDisclosure();
      this.closeSearchModal();
    }

    reveal() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-sticky', 'animate');
      this.header.classList.remove('shopify-section-header-hidden');
    }

    reset() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.remove('shopify-section-header-hidden', 'shopify-section-header-sticky', 'animate');
    }

    closeMenuDisclosure() {
      this.disclosures = this.disclosures || this.header.querySelectorAll('header-menu');
      this.disclosures.forEach((disclosure) => disclosure.close());
    }

    closeSearchModal() {
      this.searchModal = this.searchModal || this.header.querySelector('details-modal');
      this.searchModal.close(false);
    }
  }

  customElements.define('sticky-header', StickyHeader);
{% endjavascript %}

<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Organization",
    "name": {{ shop.name | json }},
    {% if settings.logo %}
      "logo": {{ settings.logo | image_url: width: 500 | prepend: "https:" | json }},
    {% endif %}
    "sameAs": [
      {{ settings.social_twitter_link | json }},
      {{ settings.social_facebook_link | json }},
      {{ settings.social_pinterest_link | json }},
      {{ settings.social_instagram_link | json }},
      {{ settings.social_tiktok_link | json }},
      {{ settings.social_tumblr_link | json }},
      {{ settings.social_snapchat_link | json }},
      {{ settings.social_youtube_link | json }},
      {{ settings.social_vimeo_link | json }}
    ],
    "url": {{ request.origin | append: page.url | json }}
  }
</script>

{%- if request.page_type == 'index' -%}
  {% assign potential_action_target = request.origin | append: routes.search_url | append: '?q={search_term_string}' %}
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "WebSite",
      "name": {{ shop.name | json }},
      "potentialAction": {
        "@type": "SearchAction",
        "target": {{ potential_action_target | json }},
        "query-input": "required name=search_term_string"
      },
      "url": {{ request.origin | append: page.url | json }}
    }
  </script>
{%- endif -%}

{% schema %}
{
  "name": "t:sections.header.name",
  "class": "section-header",
  "max_blocks": 3,
  "settings": [
    {
      "type": "select",
      "id": "logo_position",
      "options": [
        {
          "value": "top-left",
          "label": "t:sections.header.settings.logo_position.options__2.label"
        },
        {
          "value": "top-center",
          "label": "t:sections.header.settings.logo_position.options__3.label"
        },
        {
          "value": "middle-left",
          "label": "t:sections.header.settings.logo_position.options__1.label"
        },
        {
          "value": "middle-center",
          "label": "t:sections.header.settings.logo_position.options__4.label"
        }
      ],
      "default": "middle-left",
      "label": "t:sections.header.settings.logo_position.label",
      "info": "t:sections.header.settings.logo_help.content"
    },
    {
      "type": "select",
      "id": "mobile_logo_position",
      "options": [
        {
          "value": "center",
          "label": "t:sections.header.settings.mobile_logo_position.options__1.label"
        },
        {
          "value": "left",
          "label": "t:sections.header.settings.mobile_logo_position.options__2.label"
        }
      ],
      "default": "center",
      "label": "t:sections.header.settings.mobile_logo_position.label"
    },
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:sections.header.settings.menu.label"
    },
    {
      "type": "select",
      "id": "menu_type_desktop",
      "options": [
        {
          "value": "dropdown",
          "label": "t:sections.header.settings.menu_type_desktop.options__1.label"
        },
        {
          "value": "mega",
          "label": "t:sections.header.settings.menu_type_desktop.options__2.label"
        },
        {
          "value": "drawer",
          "label": "t:sections.header.settings.menu_type_desktop.options__3.label"
        }
      ],
      "default": "dropdown",
      "label": "t:sections.header.settings.menu_type_desktop.label"
    },
    {
      "type": "header",
      "content": "Navigation typography"
    },
    {
      "type": "checkbox",
      "id": "nav_enable_type",
      "default": false,
      "label": "Enable custom nav typography",
      "info": "Applies to desktop navigation. Style each level below; leave a control at its default to inherit."
    },
    {
      "type": "header",
      "content": "Main nav (top level)"
    },
    {
      "type": "select",
      "id": "mainnav_custom_font",
      "options": [
        { "value": "default", "label": "None (inherit theme)" },
        { "value": "ibm_plex_sans", "label": "IBM Plex Sans" },
        { "value": "ibm_plex_mono", "label": "IBM Plex Mono" },
        { "value": "inter", "label": "Inter" },
        { "value": "geist", "label": "Geist" },
        { "value": "geist_mono", "label": "Geist Mono" },
        { "value": "overpass", "label": "Overpass" },
        { "value": "overpass_mono", "label": "Overpass Mono" },
        { "value": "dm_sans", "label": "DM Sans" },
        { "value": "dm_mono", "label": "DM Mono" },
        { "value": "manrope", "label": "Manrope" },
        { "value": "open_sans", "label": "Open Sans" }
      ],
      "default": "default",
      "label": "Brand font",
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "range",
      "id": "mainnav_font_size",
      "min": 0,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Font size",
      "info": "0 = theme default (13px)",
      "default": 0,
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "select",
      "id": "mainnav_font_weight",
      "options": [
        { "value": "default", "label": "Theme default" },
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semibold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "default",
      "label": "Font weight",
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "range",
      "id": "mainnav_letter_spacing",
      "min": 0,
      "max": 4,
      "step": 0.1,
      "unit": "px",
      "label": "Letter spacing",
      "info": "0 = theme default",
      "default": 0,
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "select",
      "id": "mainnav_text_case",
      "options": [
        { "value": "default", "label": "Theme default" },
        { "value": "uppercase", "label": "Uppercase" },
        { "value": "capitalize", "label": "Capitalize" },
        { "value": "lowercase", "label": "Lowercase" },
        { "value": "none", "label": "Normal (remove uppercase)" }
      ],
      "default": "default",
      "label": "Text case",
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "header",
      "content": "Sub-nav headers (2nd level)"
    },
    {
      "type": "select",
      "id": "subhead_custom_font",
      "options": [
        { "value": "default", "label": "None (inherit theme)" },
        { "value": "ibm_plex_sans", "label": "IBM Plex Sans" },
        { "value": "ibm_plex_mono", "label": "IBM Plex Mono" },
        { "value": "inter", "label": "Inter" },
        { "value": "geist", "label": "Geist" },
        { "value": "geist_mono", "label": "Geist Mono" },
        { "value": "overpass", "label": "Overpass" },
        { "value": "overpass_mono", "label": "Overpass Mono" },
        { "value": "dm_sans", "label": "DM Sans" },
        { "value": "dm_mono", "label": "DM Mono" },
        { "value": "manrope", "label": "Manrope" },
        { "value": "open_sans", "label": "Open Sans" }
      ],
      "default": "default",
      "label": "Brand font",
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "range",
      "id": "subhead_font_size",
      "min": 0,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Font size",
      "info": "0 = theme default",
      "default": 0,
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "select",
      "id": "subhead_font_weight",
      "options": [
        { "value": "default", "label": "Theme default" },
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semibold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "default",
      "label": "Font weight",
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "range",
      "id": "subhead_letter_spacing",
      "min": 0,
      "max": 4,
      "step": 0.1,
      "unit": "px",
      "label": "Letter spacing",
      "info": "0 = theme default",
      "default": 0,
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "select",
      "id": "subhead_text_case",
      "options": [
        { "value": "default", "label": "Theme default" },
        { "value": "uppercase", "label": "Uppercase" },
        { "value": "capitalize", "label": "Capitalize" },
        { "value": "lowercase", "label": "Lowercase" },
        { "value": "none", "label": "Normal (remove uppercase)" }
      ],
      "default": "default",
      "label": "Text case",
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "header",
      "content": "Sub-nav items (3rd level)"
    },
    {
      "type": "select",
      "id": "subitem_custom_font",
      "options": [
        { "value": "default", "label": "None (inherit theme)" },
        { "value": "ibm_plex_sans", "label": "IBM Plex Sans" },
        { "value": "ibm_plex_mono", "label": "IBM Plex Mono" },
        { "value": "inter", "label": "Inter" },
        { "value": "geist", "label": "Geist" },
        { "value": "geist_mono", "label": "Geist Mono" },
        { "value": "overpass", "label": "Overpass" },
        { "value": "overpass_mono", "label": "Overpass Mono" },
        { "value": "dm_sans", "label": "DM Sans" },
        { "value": "dm_mono", "label": "DM Mono" },
        { "value": "manrope", "label": "Manrope" },
        { "value": "open_sans", "label": "Open Sans" }
      ],
      "default": "default",
      "label": "Brand font",
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "range",
      "id": "subitem_font_size",
      "min": 0,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Font size",
      "info": "0 = theme default (13px)",
      "default": 0,
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "select",
      "id": "subitem_font_weight",
      "options": [
        { "value": "default", "label": "Theme default" },
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semibold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "default",
      "label": "Font weight",
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "range",
      "id": "subitem_letter_spacing",
      "min": 0,
      "max": 4,
      "step": 0.1,
      "unit": "px",
      "label": "Letter spacing",
      "info": "0 = theme default",
      "default": 0,
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    {
      "type": "select",
      "id": "subitem_text_case",
      "options": [
        { "value": "default", "label": "Theme default" },
        { "value": "uppercase", "label": "Uppercase" },
        { "value": "capitalize", "label": "Capitalize" },
        { "value": "lowercase", "label": "Lowercase" },
        { "value": "none", "label": "Normal (remove uppercase)" }
      ],
      "default": "default",
      "label": "Text case",
      "visible_if": "{{ section.settings.nav_enable_type }}"
    },
    { "type": "header", "content": "Mega menu spacing" },
    { "type": "range", "id": "megamenu_row_gap", "min": 0, "max": 60, "step": 2, "unit": "px", "label": "Row gap", "info": "0 = theme default (1.8rem)", "default": 0 },
    { "type": "range", "id": "megamenu_col_gap", "min": 0, "max": 120, "step": 4, "unit": "px", "label": "Column gap", "info": "0 = theme default (4rem)", "default": 0 },
    { "type": "header", "content": "Mobile drawer typography" },
    { "type": "checkbox", "id": "drawer_enable_type", "default": false, "label": "Enable custom drawer typography", "info": "Applies to the mobile/drawer menu. Style each level below; leave a control at its default to inherit." },
    { "type": "header", "content": "Drawer level 1" },
    { "type": "select", "id": "drawer_l1_custom_font", "options": [ { "value": "default", "label": "None (inherit theme)" }, { "value": "ibm_plex_sans", "label": "IBM Plex Sans" }, { "value": "ibm_plex_mono", "label": "IBM Plex Mono" }, { "value": "inter", "label": "Inter" }, { "value": "geist", "label": "Geist" }, { "value": "geist_mono", "label": "Geist Mono" }, { "value": "overpass", "label": "Overpass" }, { "value": "overpass_mono", "label": "Overpass Mono" }, { "value": "dm_sans", "label": "DM Sans" }, { "value": "dm_mono", "label": "DM Mono" }, { "value": "manrope", "label": "Manrope" }, { "value": "open_sans", "label": "Open Sans" } ], "default": "overpass", "label": "Brand font", "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l1_font_size", "min": 0, "max": 24, "step": 1, "unit": "px", "label": "Font size", "info": "0 = theme default (1.8rem)", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "select", "id": "drawer_l1_font_weight", "options": [ { "value": "default", "label": "Theme default" }, { "value": "300", "label": "Light" }, { "value": "400", "label": "Regular" }, { "value": "500", "label": "Medium" }, { "value": "600", "label": "Semibold" }, { "value": "700", "label": "Bold" } ], "default": "700", "label": "Font weight", "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l1_letter_spacing", "min": 0, "max": 4, "step": 0.1, "unit": "px", "label": "Letter spacing", "info": "0 = theme default", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "select", "id": "drawer_l1_text_case", "options": [ { "value": "default", "label": "Theme default" }, { "value": "uppercase", "label": "Uppercase" }, { "value": "capitalize", "label": "Capitalize" }, { "value": "lowercase", "label": "Lowercase" }, { "value": "none", "label": "Normal (remove uppercase)" } ], "default": "default", "label": "Text case", "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l1_indent", "min": 0, "max": 60, "step": 2, "unit": "px", "label": "Indent", "info": "Base left inset for every drawer item; levels 2-3 add their own indent. 0 = theme default (3rem)", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l1_v_space", "min": 0, "max": 24, "step": 1, "unit": "px", "label": "Item vertical spacing", "info": "0 = theme default (1.1rem)", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "header", "content": "Drawer level 2" },
    { "type": "select", "id": "drawer_l2_custom_font", "options": [ { "value": "default", "label": "None (inherit theme)" }, { "value": "ibm_plex_sans", "label": "IBM Plex Sans" }, { "value": "ibm_plex_mono", "label": "IBM Plex Mono" }, { "value": "inter", "label": "Inter" }, { "value": "geist", "label": "Geist" }, { "value": "geist_mono", "label": "Geist Mono" }, { "value": "overpass", "label": "Overpass" }, { "value": "overpass_mono", "label": "Overpass Mono" }, { "value": "dm_sans", "label": "DM Sans" }, { "value": "dm_mono", "label": "DM Mono" }, { "value": "manrope", "label": "Manrope" }, { "value": "open_sans", "label": "Open Sans" } ], "default": "overpass", "label": "Brand font", "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l2_font_size", "min": 0, "max": 24, "step": 1, "unit": "px", "label": "Font size", "info": "0 = theme default (1.8rem)", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "select", "id": "drawer_l2_font_weight", "options": [ { "value": "default", "label": "Theme default" }, { "value": "300", "label": "Light" }, { "value": "400", "label": "Regular" }, { "value": "500", "label": "Medium" }, { "value": "600", "label": "Semibold" }, { "value": "700", "label": "Bold" } ], "default": "default", "label": "Font weight", "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l2_letter_spacing", "min": 0, "max": 4, "step": 0.1, "unit": "px", "label": "Letter spacing", "info": "0 = theme default", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "select", "id": "drawer_l2_text_case", "options": [ { "value": "default", "label": "Theme default" }, { "value": "uppercase", "label": "Uppercase" }, { "value": "capitalize", "label": "Capitalize" }, { "value": "lowercase", "label": "Lowercase" }, { "value": "none", "label": "Normal (remove uppercase)" } ], "default": "default", "label": "Text case", "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l2_indent", "min": 0, "max": 60, "step": 2, "unit": "px", "label": "Indent", "info": "Offset of 2nd-level submenus, applied to each of the two nested wrappers. 0 = flush", "default": 20, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l2_v_space", "min": 0, "max": 24, "step": 1, "unit": "px", "label": "Item vertical spacing", "info": "0 = theme default (1.1rem)", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "header", "content": "Drawer level 3" },
    { "type": "select", "id": "drawer_l3_custom_font", "options": [ { "value": "default", "label": "None (inherit theme)" }, { "value": "ibm_plex_sans", "label": "IBM Plex Sans" }, { "value": "ibm_plex_mono", "label": "IBM Plex Mono" }, { "value": "inter", "label": "Inter" }, { "value": "geist", "label": "Geist" }, { "value": "geist_mono", "label": "Geist Mono" }, { "value": "overpass", "label": "Overpass" }, { "value": "overpass_mono", "label": "Overpass Mono" }, { "value": "dm_sans", "label": "DM Sans" }, { "value": "dm_mono", "label": "DM Mono" }, { "value": "manrope", "label": "Manrope" }, { "value": "open_sans", "label": "Open Sans" } ], "default": "inter", "label": "Brand font", "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l3_font_size", "min": 0, "max": 24, "step": 1, "unit": "px", "label": "Font size", "info": "0 = theme default (1.8rem)", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "select", "id": "drawer_l3_font_weight", "options": [ { "value": "default", "label": "Theme default" }, { "value": "300", "label": "Light" }, { "value": "400", "label": "Regular" }, { "value": "500", "label": "Medium" }, { "value": "600", "label": "Semibold" }, { "value": "700", "label": "Bold" } ], "default": "default", "label": "Font weight", "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l3_letter_spacing", "min": 0, "max": 4, "step": 0.1, "unit": "px", "label": "Letter spacing", "info": "0 = theme default", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "select", "id": "drawer_l3_text_case", "options": [ { "value": "default", "label": "Theme default" }, { "value": "uppercase", "label": "Uppercase" }, { "value": "capitalize", "label": "Capitalize" }, { "value": "lowercase", "label": "Lowercase" }, { "value": "none", "label": "Normal (remove uppercase)" } ], "default": "default", "label": "Text case", "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l3_indent", "min": 0, "max": 60, "step": 2, "unit": "px", "label": "Indent", "info": "Extra offset of 3rd-level submenus. 0 = flush", "default": 20, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    { "type": "range", "id": "drawer_l3_v_space", "min": 0, "max": 24, "step": 1, "unit": "px", "label": "Item vertical spacing", "info": "0 = theme default (1.1rem)", "default": 0, "visible_if": "{{ section.settings.drawer_enable_type }}" },
    {
      "type": "select",
      "id": "sticky_header_type",
      "options": [
        {
          "value": "none",
          "label": "t:sections.header.settings.sticky_header_type.options__1.label"
        },
        {
          "value": "on-scroll-up",
          "label": "t:sections.header.settings.sticky_header_type.options__2.label"
        },
        {
          "value": "always",
          "label": "t:sections.header.settings.sticky_header_type.options__3.label"
        },
        {
          "value": "reduce-logo-size",
          "label": "t:sections.header.settings.sticky_header_type.options__4.label"
        }
      ],
      "default": "on-scroll-up",
      "label": "t:sections.header.settings.sticky_header_type.label"
    },
    {
      "type": "checkbox",
      "id": "show_line_separator",
      "default": true,
      "label": "t:sections.header.settings.show_line_separator.label"
    },
    {
      "type": "header",
      "content": "Transparent Header"
    },
    {
      "type": "text",
      "id": "transparency_enabled_paths",
      "default": "/,/pages/child,/pages/baby,/pages/adult",
      "label": "Pages where transparency is enabled",
      "info": "Comma separated list of paths"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__1.content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "color_scheme",
      "id": "menu_color_scheme",
      "label": "t:sections.header.settings.menu_color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__utilities.content"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_country_selector.label",
      "info": "t:sections.header.settings.enable_country_selector.info"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_language_selector.label",
      "info": "t:sections.header.settings.enable_language_selector.info"
    },
    {
      "type": "checkbox",
      "id": "enable_customer_avatar",
      "default": true,
      "label": "t:sections.header.settings.enable_customer_avatar.label",
      "info": "t:sections.header.settings.enable_customer_avatar.info"
    },
    {
      "type": "header",
      "content": "t:sections.all.spacing"
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.header.settings.margin_bottom.label",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "info": "0 = theme default (10px)",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "info": "0 = theme default (10px)",
      "default": 0
    },
    { "type": "range", "id": "padding_left", "min": 0, "max": 120, "step": 2, "unit": "px", "label": "Padding left (desktop)", "info": "Applies from 750px up. 0 = theme default (3.2rem, 20px above 1100px)", "default": 0 },
    { "type": "range", "id": "padding_right", "min": 0, "max": 120, "step": 2, "unit": "px", "label": "Padding right (desktop)", "info": "Applies from 750px up. 0 = theme default (3.2rem, 20px above 1100px)", "default": 0 },
    { "type": "range", "id": "padding_left_mobile", "min": 0, "max": 60, "step": 2, "unit": "px", "label": "Padding left (mobile)", "info": "Applies below 750px. 0 = theme default (1.4rem)", "default": 0 },
    { "type": "range", "id": "padding_right_mobile", "min": 0, "max": 60, "step": 2, "unit": "px", "label": "Padding right (mobile)", "info": "Applies below 750px. 0 = theme default (1.4rem)", "default": 0 },
    { "type": "header", "content": "Design review" },
    {
      "type": "text",
      "id": "pin_mega_menu_handle",
      "label": "Pin mega menu open (editor only)",
      "info": "Handle of a top-level menu item (e.g. 'shop') to hold its mega menu open while editing. Blank = normal hover. No effect outside the theme editor."
    },
    { "type": "header", "content": "Search tabs" },
    { "type": "checkbox", "id": "enable_search_tabs", "default": true, "label": "Enable search category tabs", "info": "Requires the 'Line' filter in Search & Discovery" },
    { "type": "checkbox", "id": "search_tabs_show_counts", "default": true, "label": "Show result counts on tabs" },
    { "type": "text", "id": "search_tabs_all_label", "default": "All", "label": "'All' tab label" },
    { "type": "text", "id": "search_tabs_values", "default": "Child,Baby,Adult,Studio,Home", "label": "Tabs", "info": "Comma-separated Line values (must match Search & Discovery values exactly). A tab only shows when it has results." }
  ],
  "blocks": [
    {
      "type": "menu_images",
      "name": "Mega-menu images",
      "settings": [
        {
          "type": "text",
          "id": "menu_item_handle",
          "label": "Top-level menu item handle",
          "info": "The handle of the top-level menu item to attach images to (e.g. 'shop', 'outerwear'). Find it under Content > Navigation."
        },
        {
          "type": "header",
          "content": "Image 1"
        },
        {
          "type": "image_picker",
          "id": "image_1",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "caption_1",
          "label": "Caption"
        },
        {
          "type": "url",
          "id": "link_1",
          "label": "Link"
        },
        {
          "type": "header",
          "content": "Image 2 (optional)"
        },
        {
          "type": "image_picker",
          "id": "image_2",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "caption_2",
          "label": "Caption"
        },
        {
          "type": "url",
          "id": "link_2",
          "label": "Link"
        },
        {
          "type": "header",
          "content": "Caption typography"
        },
        {
          "type": "select",
          "id": "caption_custom_font",
          "options": [
            { "value": "default", "label": "None (inherit theme)" },
            { "value": "ibm_plex_sans", "label": "IBM Plex Sans" },
            { "value": "ibm_plex_mono", "label": "IBM Plex Mono" },
            { "value": "inter", "label": "Inter" },
            { "value": "geist", "label": "Geist" },
            { "value": "geist_mono", "label": "Geist Mono" },
            { "value": "overpass", "label": "Overpass" },
            { "value": "overpass_mono", "label": "Overpass Mono" },
            { "value": "dm_sans", "label": "DM Sans" },
            { "value": "dm_mono", "label": "DM Mono" },
            { "value": "manrope", "label": "Manrope" },
            { "value": "open_sans", "label": "Open Sans" }
          ],
          "default": "default",
          "label": "Brand font"
        },
        {
          "type": "range",
          "id": "caption_font_size",
          "min": 0,
          "max": 24,
          "step": 1,
          "unit": "px",
          "label": "Font size",
          "info": "0 = default (13px)",
          "default": 0
        },
        {
          "type": "select",
          "id": "caption_font_weight",
          "options": [
            { "value": "default", "label": "Theme default" },
            { "value": "300", "label": "Light" },
            { "value": "400", "label": "Regular" },
            { "value": "500", "label": "Medium" },
            { "value": "600", "label": "Semibold" },
            { "value": "700", "label": "Bold" }
          ],
          "default": "default",
          "label": "Font weight"
        },
        {
          "type": "range",
          "id": "caption_letter_spacing",
          "min": 0,
          "max": 4,
          "step": 0.1,
          "unit": "px",
          "label": "Letter spacing",
          "info": "0 = default",
          "default": 0
        },
        {
          "type": "select",
          "id": "caption_text_case",
          "options": [
            { "value": "default", "label": "Theme default" },
            { "value": "uppercase", "label": "Uppercase" },
            { "value": "capitalize", "label": "Capitalize" },
            { "value": "lowercase", "label": "Lowercase" },
            { "value": "none", "label": "Normal (remove uppercase)" }
          ],
          "default": "default",
          "label": "Text case"
        },
        {
          "type": "header",
          "content": "Image layout & spacing"
        },
        {
          "type": "range",
          "id": "image_width",
          "min": 120,
          "max": 400,
          "step": 10,
          "unit": "px",
          "label": "Image width",
          "default": 220
        },
        {
          "type": "range",
          "id": "images_gap",
          "min": 0,
          "max": 60,
          "step": 2,
          "unit": "px",
          "label": "Gap between the two images",
          "default": 20
        },
        {
          "type": "range",
          "id": "caption_gap",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "label": "Space above caption",
          "default": 8
        },
        {
          "type": "range",
          "id": "panel_gap",
          "min": 0,
          "max": 120,
          "step": 4,
          "unit": "px",
          "label": "Gap between links and images",
          "default": 40
        }
      ]
    },
    {
      "type": "@app"
    }
  ]
}
{% endschema %}
